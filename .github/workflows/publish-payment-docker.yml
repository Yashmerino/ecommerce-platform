name: Build and Push Payment Service Docker Image

on:
  push:
    branches:
      - master 

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Payment Service Docker image
      run: |
        cd ecommerce-platform-payment-service
        docker build -t ${{ secrets.DOCKER_USERNAME }}/ecommerce-platform-payment-service:latest .
        docker push ${{ secrets.DOCKER_USERNAME }}/ecommerce-platform-payment-service:latest

    - name: Logout from Docker Hub
      run: docker logout
